import FrameBox from '@/components/frame/FrameBox';
import PickerLayout from '@/components/Layouts/PickerLayout';

const FrameChoice = () => {
  return (
    <PickerLayout step='frame'>
      <FrameBox />
    </PickerLayout>
  );
};

export default FrameChoice;
